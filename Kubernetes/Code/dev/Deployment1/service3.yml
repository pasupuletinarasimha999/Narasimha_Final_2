---
apiVersion: v1
kind: Service
metadata:
  name: deploythreeservice
  namespace: my-app
  labels:
    prometheus-enabled: "true"
spec:
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  selector:
    app: nginx
  type: ClusterIP
