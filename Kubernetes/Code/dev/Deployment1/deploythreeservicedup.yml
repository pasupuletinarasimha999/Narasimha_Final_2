---
apiVersion: v1
kind: Service
metadata:
  name: deploythreeservicedup
  namespace: my-app
  labels:
    prometheus-enabled: "true"
spec:
  ports:
  - port: 80
    targetPort: 8501
    protocol: TCP
  selector:
    app: nginx
  type: LoadBalancer
