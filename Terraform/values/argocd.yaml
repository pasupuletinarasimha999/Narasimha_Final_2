---
dex:
  enabled: false

server:
  # -- Argo CD server name
  name: server
  # -- Change the service type to LoadBalancer
  service:
    type: LoadBalancer
  # Other configuration options...
  extraArgs:
    - --insecure
